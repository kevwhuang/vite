import React from 'react';

import '../styles/pages/Home.scss';

function Home(): React.ReactElement {
    return (
        <main id="home">
            <title>Home</title>
        </main>
    );
}

export default Home;
